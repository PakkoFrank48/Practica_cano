let mayor = (miArray, nAlto) => {
  let nAlto = Math.max(...miArray);
};
let miArray = [0, 1, 10, 3, 4, 5, 6, 7];
let resultado = mayor(miArray);
//console.log(resultado);
//funcion que le des un array de numeros y te diga cual es el mayor

//con fallas
