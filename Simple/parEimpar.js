let numero = (valor) => {
  if (valor & (2 == 0)) {
    return console.log("es par");
  } else {
    return console.log("es impar");
  }
};

let valor = 8;

let resultado = numero(valor);

console.log(resultado);

// falla total
