let area = (base, altura) => {
  return (base * altura) / 2;
};

let base = 8;
let altura = 8;

let resultado = area(base, altura);

console.log(resultado);

//programa que te pida la base y altura de un triangulo y te de el area
